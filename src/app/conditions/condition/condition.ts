import { City } from './city';

export class Condition {
  weatherCity: City;
  dateTime: string;
  weather: string;
  temperature: number;
  icon: string;
  icon_url: string;
  precipitation: string;
  humidite: string;
  vent: string;
}
